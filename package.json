{
  "main": "index.js",
  "dependencies": {
    "axios": "^1.3.4",
    "dotenv": "^16.0.3",
    "electron-prompt": "^1.7.0"
  },
  "devDependencies": {
    "electron": "^23.1.3"
  },
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
